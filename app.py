import streamlit as st import pandas as pd import numpy as np import requests import joblib import pvlib from datetime import datetime # 1. Page Configuration & Title Display st.set_page_config(page_title="Dubai Solar Predictor", layout="centered") st.title("☀️ Dubai Smart Solar Predictive Platform") st.write("Real-time AI forecasting mapped with geometric and thermal physics filters.") # 2. Optimized Model Loading (Prevents server slowdowns) @st.cache_resourcedef load_solar_brain(): return joblib.load('advanced_solar_brain.pkl') ai_brain = load_solar_brain() # 3. Interactive Web Sidebar Inputs (Replaces Colab input commands) st.sidebar.header("🛠️ Hardware Configuration Profile") PANEL_MAX_CAPACITY = st.sidebar.number_input("Maximum Panel Capacity (Watts)", min_value=10.0, max_value=1000.0, value=100.0, step=10.0) PANEL_SURFACE_TILT = st.sidebar.slider("Panel Tilt Angle (0° = flat, 90° = vertical)", 0, 90, 25) PANEL_SURFACE_AZIMUTH = st.sidebar.slider("Compass Facing Angle (180° = South, 90° = East)", 0, 360, 180) # Global constants for the calculation layers API_KEY = "81b04a4cf48bfc77710a24cb03e6915c" LOCATION = "Dubai" LATITUDE = 25.2048 LONGITUDE = 55.2708TEMP_COEFFICIENT = -0.0035 # 4. Fetching Real-Time Weather via API Connection url = f"https://api.openweathermap.org/data/2.5/weather?q={LOCATION}&appid={API_KEY}&units=metric" try: response = requests.get(url).json() if response.get("cod") == 200: # Extract live data vectors temp = response['main']['temp'] clouds = response['clouds']['all'] humidity = response['main']['humidity'] pressure = response['main']['pressure'] rain = response.get('rain', {}).get('1h', 0) # Display crisp metric badges across the screen col1, col2, col3 = st.columns(3) col1.metric("Temperature", f"{temp}°C") col2.metric("Cloud Cover", f"{clouds}%") col3.metric("Humidity", f"{humidity}%") # 5. Celestial Tracking Geometry Layer current_time = pd.Timestamp(datetime.utcnow(), tz='UTC') sol_pos = pvlib.solarposition.get_solarposition(current_time, LATITUDE, LONGITUDE) live_zenith = sol_pos['apparent_zenith'].iloc[0] live_azimuth = sol_pos['azimuth'].iloc[0] # Calculate Angle of Incidence (AOI) aoi = pvlib.irradiance.aoi(PANEL_SURFACE_TILT, PANEL_SURFACE_AZIMUTH, live_zenith, live_azimuth) # 6. Hybrid Predictive Calculations estimated_ghi = 950 * (1 - (0.0075 * clouds)) clues = [[estimated_ghi, clouds, temp, live_zenith, live_azimuth, humidity, pressure, rain, PANEL_MAX_CAPACITY]] raw_prediction = ai_brain.predict(clues)[0] # Apply physical orientation modifier geometric_efficiency = np.cos(np.radians(aoi)) if geometric_efficiency < 0: geometric_efficiency = 0.0 # Apply thermal degradation modifier heat_loss_multiplier = 1.0 if temp > 25: heat_loss_multiplier = 1.0 + (TEMP_COEFFICIENT * (temp - 25)) # Process composite output predictionstailored_wattage = raw_prediction * geometric_efficiency * heat_loss_multiplier # Structural boundary checks iftailored_wattage > PANEL_MAX_CAPACITY: tailored_wattage = PANEL_MAX_CAPACITY if tailored_wattage < 0: tailored_wattage = 0.0 # 7. Rendering UI Output Layout Elements st.markdown("---") st.subheader("📊 Live Predictive System Output") st.success(f"### **Estimated Generation: {tailored_wattage:.2f} Watts**") if aoi < 45: st.info("🎯 **Excellent Alignment:** The sunbeams are hitting your panel coordinates efficiently.") else: st.warning("⚠️ **High Geometric Mismatch:** Structural angles are causing reflection losses.") else: st.error(f"Weather API Error: {response.get('message')}") except Exception as e: st.error(f"Server Connection Failure: {e}")